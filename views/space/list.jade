extends ../layout

block title
  title #{sitetitle} | Available Spaces

block content

  div.container

    h1 Available Spaces

    ul.list-unstyled
      each space in spaces
        li 
          a(href="/space/view/#{space.id}") #{space.address}
          |, #{space.zip} - #{space.contact}
