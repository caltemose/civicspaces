extends layout

block title
  title #{sitetitle} | Login

block content

  div.container

    h1 Sign Up

    div.row
      div.col-md-5
        
        if session.isLoggedIn
          p You cannot sign up for a new account while logged into an existing account.

        else

          form(method="POST" action="/signup" enctype='application/x-www-form-urlencoded')
            
            p Create a CivicSpaces account using this form. You must have an account in order to list your own space on this site.
            
            p
              strong All fields are required.

            if exists
              .error
                | That user exists. Are you trying to 
                a(href="/login") Log in
                | ?

            if invalid
              .error Please enter a valid email address and password.

            label
              | Email:
              input.form-control(type="text" name="email") 

            label
              | Name:
              input.form-control(type="text" name="name") 

            label
              | Phone number:
              input.form-control(type="text" name="phone") 

            label
              | Password:
              input.form-control(type="password" name="pass")

            input.btn.btn-primary.drop(type="submit" name="submit" value="Create Account") 

          p
            a(href="/login") Log in
            |  if you already have an account.
